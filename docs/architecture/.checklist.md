# Architecture Documentation Checklist

Working document — tracks every item that requires human input before this documentation is complete. Delete entries as they are resolved.

Generated: 2026-05-20

---

## 1. Confirm dates on migrated ADRs

The original `docs/decisions/` files did not record decision dates. Update each migrated ADR with the actual date the decision was made.

| File                                          | Field       |
| --------------------------------------------- | ----------- |
| `adr/0002-spa-over-ssr.md`                    | `**Date:**` |
| `adr/0003-use-tailwind-css-v4.md`             | `**Date:**` |
| `adr/0004-sanity-as-headless-cms.md`          | `**Date:**` |
| `adr/0005-resend-for-email-delivery.md`       | `**Date:**` |
| `adr/0006-vercel-for-deployment.md`           | `**Date:**` |
| `adr/0007-vitest-rtl-msw-testing-strategy.md` | `**Date:**` |

---

## 2. Review and accept inferred ADRs

Two ADRs were inferred from the codebase. They have status `Proposed` and carry the ⚠️ INFERRED banner. Review each one, fill in the Rationale if it differs from what is written, then change the status to `Accepted` (or `Rejected` if the decision is being reconsidered).

| File                                           | Inferred decision                                | Action required                                      |
| ---------------------------------------------- | ------------------------------------------------ | ---------------------------------------------------- |
| `adr/0008-framer-motion-for-animation.md`      | Framer Motion chosen for animation               | Review rationale, set status to Accepted or Rejected |
| `adr/0009-shared-zod-schema-for-validation.md` | Shared Zod schema for client + server validation | Review rationale, set status to Accepted or Rejected |

---

## 3. Confirm the live production URL

Two files reference `https://josephsearle.dev` with a `<!-- TODO: confirm live URL -->` comment:

- `05-deployment.md` — Environments table, Production row
- `README.md` → links to `05-deployment.md`

Update the URL once confirmed, or remove the comment if the URL is correct.

---

## 4. Fill in measurable quality targets

`07-quality-and-nfrs.md` contains several `TBD` values in the Quality goals table. Replace each with a real measurement once baseline metrics have been captured.

| Metric        | Current value               | Action                                                                                  |
| ------------- | --------------------------- | --------------------------------------------------------------------------------------- |
| Availability  | `TBD — inherits Vercel SLA` | Decide whether an explicit SLA is needed; if not, mark as "No SLA — personal portfolio" |
| Test coverage | `> 80% line coverage`       | Run `npm run test:coverage` and record the actual baseline                              |

---

## 5. Add a Content Security Policy

`07-quality-and-nfrs.md` flags the absence of CSP headers as a security gap. To address:

1. Add a `headers` block to `vercel.json` with a `Content-Security-Policy` header
2. Update `07-quality-and-nfrs.md` Security baseline table — remove the gap note once done

---

## 6. Add a CI pipeline

`05-deployment.md` and `09-risks-and-debt.md` both note that there is no GitHub Actions workflow running lint and tests on PRs. To address:

1. Create `.github/workflows/ci.yml` running `npm run lint && npm run test:run` on `pull_request`
2. Update `05-deployment.md` CI/CD table — replace the `<!-- TODO -->` comments with the actual steps

---

## 7. Remove or archive docs/decisions/

The six original ADRs in `docs/decisions/` have been migrated to `docs/architecture/adr/0002–0007`. Once the migrated ADRs are reviewed and accepted:

1. Confirm the migrated files are correct
2. Delete the `docs/decisions/` directory (or add a redirect notice pointing to the new location)
3. Update any external links that pointed to `docs/decisions/`

---

## 8. Update docs/architecture.md

The flat `docs/architecture.md` file predates this docs suite and overlaps with several sections (overview, component hierarchy, testing strategy, styling conventions). Options:

- **Replace** with a one-paragraph redirect: `"Architecture documentation has moved to docs/architecture/. See README.md for navigation."`
- **Keep** as a narrative companion to the structured docs suite (it contains useful detail for new contributors)

The README "Further reading" section has already been updated to point to `docs/architecture/README.md`.
